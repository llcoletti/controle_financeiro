import React from 'react';
import * as HeaderStyles from './HeaderStyle';

const Header = () => {
  return (
    <HeaderStyles.Container>
      <HeaderStyles.Header>
        <HeaderStyles.Title>Controle Financeiro</HeaderStyles.Title>
      </HeaderStyles.Header>
    </HeaderStyles.Container>
  );
};

export default Header;
